x1 = (RES_W * RES_SCALE) / 2;
y1 = (RES_H * RES_SCALE) - 270;
x2 = (RES_W * RES_SCALE) / 2;
y2  = (RES_H * RES_SCALE) - 20;

x1_target = 8;
x2_target = (RES_W * RES_SCALE) - 8;

lerp_progress = 0;
text_progress = 0;

response_selected = 0;